{
  "services": {
    "web": {
      "root": "web/",
      "framework": "nextjs",
      "installCommand": "cd .. && npm install",
      "buildCommand": "cd .. && npm run build -w @sidepad/shared && npm run build -w @sidepad/web",
      "bindings": [
        {
          "type": "service",
          "service": "backend",
          "format": "url",
          "env": "BACKEND_URL"
        }
      ]
    },
    "backend": {
      "root": "backend/",
      "entrypoint": "app.main:app"
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": { "service": "backend" } },
    { "source": "/(.*)", "destination": { "service": "web" } }
  ]
}
